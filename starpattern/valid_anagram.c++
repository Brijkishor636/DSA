#include <iostream>
#include <string.h>
#include <cstring>
using namespace std;

class Solution {
    public:
        bool isAnagram(string s, string t) {
            if(s.length() == t.length()){
                for (int i = 0; i < s.length(); i++)
                {
                    
                }
                
            }
            
        }
};

int main(){
    Solution s1;
    bool temp = s1.isAnagram("sljksj", "sljdfsj");
    cout<<temp;
}